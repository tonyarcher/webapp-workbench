# Deliberately not `root = true`: the repo-level .editorconfig carries the
# ktlint rule scoping and line-length settings. Root stays authoritative.
[*]
charset = utf-8
indent_style = space
indent_size = 4
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
