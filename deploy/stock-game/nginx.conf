# nginx config for the stock game. The gateway strips the /stock-game/ prefix
# before proxying, so this server serves the built app at the root path.
server {
    listen 3000;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/javascript image/svg+xml;
    gzip_min_length 1024;

    # Never cache the SPA shell so updates propagate immediately
    location = /index.html {
        add_header Cache-Control "no-cache";
        try_files $uri =404;
    }

    # SPA fallback: any path that isn't a real file resolves to index.html.
    # Routes live behind the hash, so deep links always land on the shell.
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Long-cache hashed build assets
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }
}
