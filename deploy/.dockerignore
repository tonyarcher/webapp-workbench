# Cert install leftovers. nginx only needs leaf.crt and leaf.key; a previous
# private key at leaf.key.bak must not be baked into the gateway image.
gateway/certs/*.bak
gateway/certs/*.work
gateway/certs/*.installing
